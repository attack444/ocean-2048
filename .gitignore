node_modules/
www/
dist/
build/
# ZIP-артефакты сборок (VK/Yandex) — не коммитить
build.zip
*.zip
.DS_Store
*.log
.idea/
.vscode/

# Профили Playwright/Chromium, создаваемые при генерации скриншотов
store/shots/.cdptest/
store/shots/.chrome-*/
store/shots/.shot-*/
.vscode/
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/captures/
ios/App/Pods/
ios/DerivedData/
*.keystore
*.jks
!debug.keystore

# Локальное окружение на диске D: (JDK, Android SDK, gradle-кэш, keystore, пароли)
tools/
.env
.env.*

# Contains SSH credentials + GitHub token — never commit
docs/DEPLOY-NOTES.md

# Local checkpoint notes — never commit
docs/PROGRESS-NOTES.md

# GitHub automation helper scripts — never commit (may contain credentials)
gh-*.js

# VK Mini Apps deploy config — contains App ID (не коммитить в публичный репозиторий)
vk-hosting-config.json
.aider*
